const conf = {
    appwriteUrl: String(
        import.meta.env.VIRE_APPWRITE_URL),
    appwriteProjectId: String(
        import.meta.env.VIRE_PROJECT_ID),
    appwriteDataBaseId: String(
        import.meta.env.VIRE_DATABASE_ID),
    appwriteCollectionId: String(
        import.meta.env.VIRE_COLLECTION_ID),
    appwriteBucketId: String(
        import.meta.env.VIRE_BUCKET_ID),
};